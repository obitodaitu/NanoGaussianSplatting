
[To Do list]



***再對比一次Nanite, spark, supersplat的設計
***測試多種PLY，兼容性問題 > 沒有SH資料就補0，mapping才不會錯
***整理筆記
***Code review > not architecture related
***Demo - 野柳+小馬
***先做一個tile切割器，再開nanite跟level streaming就好,地形肯定要切成多塊單元，不管是asset還是地形邏輯其實都一樣
***條列有開放出來的重要API > list what feature exposed to blueprint
**************************************************************************************************


This is the Unreal Engine plugin I created "D:\GitHub\UEGaussianSplatting\Plugins\GaussianSplatting".
It can import and render Gaussian Splatting PLY file inside Unreal Engine.

However the SH Render has some issue.
When looking at the asset at lower angle, it is a bit green.
When looking at the asset at higher angle, it is a bit red.
If I turned off SH Render then the color looks corect.
Can you investigate what happened?
Just investigate and don't modify the code.
I learn the gaussian splatting render from the Unity plugin.
Use it as the reference and see is there any difference between SH Render.
D:\GitHub\UEGaussianSplatting\UnityGaussianSplatting
Make sure to take Unreal Engine's coordinate and Unity's coordinate into consideration.
Maybe check the data mapping as well.
Previously our data mapping from PLY to uasset to GPU render used to has some error.
We fixed a lot of time.



----------------------------------------------------------------------------------
  Investigation Summary: SH Rendering Color Shift Issue

  Symptom

  - Lower viewing angle (camera below, looking up): GREEN tint
  - Higher viewing angle (camera above, looking down): RED tint
  - Colors correct when SH rendering is disabled

  Key Comparison: Unity vs UE Approach

  Unity's Approach:
  1. PLY Import: No coordinate transformation - PLY data used AS-IS (PLY space = Unity space)
  2. Runtime: View direction transformed to object space via _MatrixWorldToObject matrix:
  float3 objViewDir = mul((float3x3)_MatrixWorldToObject, worldViewDir);
  3. SH Evaluation: Direction negated inside ShadeSH:
  dir *= -1;
  res += SH_C1 * (-splat.sh1 * y + splat.sh2 * z - splat.sh3 * x);

  UE's Approach:
  1. PLY Import: Positions transformed from PLY to UE space:
    - UE.X = PLY.Z (forward)
    - UE.Y = PLY.X (right)
    - UE.Z = -PLY.Y (up, negated)
    - SH coefficients NOT transformed (remain in PLY space)
  2. Runtime: View direction transformed via hardcoded coordinate mapping:
  float3 viewDirPLY = float3(viewDirWorld.y, -viewDirWorld.z, viewDirWorld.x);
  viewDirPLY = -viewDirPLY;
  3. SH Evaluation:
  result += SH_C1 * (-y * sh[0] + z * sh[1] - x * sh[2]);

  What I Verified as Correct

  1. SH coefficient storage order: Both Unity and UE convert from PLY's planar format (R0-R14, G0-G14, B0-B14) to
  interleaved format (R0G0B0, R1G1B1, ...). ✓
  2. Coordinate transformation math: The inverse mapping (UE.Y, -UE.Z, UE.X) correctly converts UE world direction to
  PLY space. ✓
  3. SH evaluation formula: Both use identical formula: SH_C1 * (-y * sh0 + z * sh1 - x * sh2). ✓
  4. Direction negation: Both end up using camera→splat direction for evaluation. ✓

  Potential Issue Found

  Critical Difference: Unity transforms the view direction using the actual object transformation matrix
  (_MatrixWorldToObject), while UE uses a hardcoded coordinate transformation that:

  1. Does NOT use LocalToWorld inverse for direction transformation
  2. Only accounts for PLY→UE coordinate system change
  3. Does NOT account for any rotation applied to the GaussianSplat actor/component in the scene

  The comment in CalcViewData.usf (line 168) explicitly states this limitation:
  // NOTE: This assumes the asset has no rotation in UE (identity LocalToWorld rotation)

  However, even with identity rotation, there could be another issue...

  Deeper Investigation Needed

  The vertical color shift (red↔green) specifically affecting the Y-component suggests one of these possibilities:

  1. Sign error in view direction transformation: The Y-component mapping -viewDirWorld.z might have incorrect sign for
  certain edge cases
  2. Mismatch in SH coefficient interpretation: The SH band 1 coefficients might need to be reordered or sign-flipped
  when the coordinate system changes (though mathematically this shouldn't be needed if view direction is transformed
  correctly)
  3. LocalToWorld not being identity: Even if the asset appears at default position, the LocalToWorld matrix might have
  a baked-in rotation from the import process that the hardcoded formula doesn't account for
  4. The transformation should use LocalToWorld inverse: Instead of the hardcoded formula, the view direction should be
  transformed using:
  float3 viewDirLocal = mul((float3x3)transpose(LocalToWorld), viewDirWorld);
  4. And then apply the PLY→UE coordinate conversion to that local direction (or incorporate it into the matrix).

  Would you like me to:
  1. Check if LocalToWorld actually contains only the PLY→UE coordinate conversion?
  2. Compare the exact LocalToWorld matrix UE uses vs what Unity's equivalent would be?
  3. Look at whether there's a 90-degree or other rotation baked into the asset that the hardcoded formula isn't
  accounting for?


----------------------------------------------------------------------------------------------

[SH function not working]
	- only f_dc is working
	- f_rest is not working


[Smaller VRAM]
	- VRAM看能不能再壓縮，目前100隻Llama VRAM會到11GB
	- sparkjs example
	- Unity plugin
	- unity article "https://aras-p.info/blog/2023/09/13/Making-Gaussian-Splats-smaller/"
	- run memory insight
	

-----------------------------------------------------------------------------------------
[Cluster的結構長這樣]
  For your 719k splat asset, approximate structure:
  ┌───────────┬──────────┬─────────────────────┐
  │ LOD Level │ Clusters │     Description     │
  ├───────────┼──────────┼─────────────────────┤
  │ 0 (leaf)  │ ~5617    │ Original splats     │
  ├───────────┼──────────┼─────────────────────┤
  │ 1         │ ~702     │ First merged level  │
  ├───────────┼──────────┼─────────────────────┤
  │ 2         │ ~88      │ Second merged level │
  ├───────────┼──────────┼─────────────────────┤
  │ 3         │ ~11      │ Third merged level  │
  ├───────────┼──────────┼─────────────────────┤
  │ 4         │ ~2       │ Fourth merged level │
  ├───────────┼──────────┼─────────────────────┤
  │ 5         │ 1        │ Root cluster        │
  └───────────┴──────────┴─────────────────────┘

  For your 1.5M splat asset, approximate structure:
  ┌───────────┬───────────┬─────────────────────┐
  │ LOD Level │ Clusters  │     Description     │
  ├───────────┼───────────┼─────────────────────┤
  │ 0 (leaf)  │ ~11718    │ Original splats     │
  ├───────────┼───────────┼─────────────────────┤
  │ 1         │ ~1464     │ First merged level  │
  ├───────────┼───────────┼─────────────────────┤
  │ 2         │ ~183      │ Second merged level │
  ├───────────┼───────────┼─────────────────────┤
  │ 3         │ ~22       │ Third merged level  │
  ├───────────┼───────────┼─────────────────────┤
  │ 4         │ ~2        │ Fourth merged level │
  ├───────────┼───────────┼─────────────────────┤
  │ 5         │ 1         │ Root cluster        │
  └───────────┴───────────┴─────────────────────┘


[每個cluster點的數量長這樣]
  LOD Level 0 (Leaf):     128 original splats per cluster
  LOD Level 1:            128/4 = 32 LOD splats per cluster (merge 4 splats → 1)
  LOD Level 2:            32/4 = 8 LOD splats per cluster
  LOD Level 3:            8/4 = 2 LOD splats per cluster
  LOD Level 4:            2/4 = 1 LOD splat per cluster

-----------------------------------------------------------------------------------------

[GlobalAccumulator]
	- bindless rendering: add more details
	- indirect rendering: add more details


[Screen Space Projected Error]
	*這一段有很大的機率有問題
	- Error = How much the LOD version is "wrong" compared to the original
	- LOD splat是相較於leaf cluster高階的點，原理是將leaf cluster的點merge成25%
	- merge的原理是平均position, rotation, color...2.3等資料
	- world space error: 原始點跟merge點相差的距離(看有多error)
	- screen space error: world space error做screen space轉換。我們在乎的是screen space的差異有多大而不是world space
	- Close > large projected error > low LOD
	- Far > small projected error > high LOD
	- MaxError = The actual error value STORED in each cluster (computed during import)
	- ErrorThreshold = The acceptable limit SET by user (runtime setting)
	- screen space error是在runtime下計算的
	- ErrorThreshold > 就是可以接受的範圍
	- MaxError跟ErrorThreshold有點問題
	- 每個cluster都會有MaxError
	- MaxError的數值是蠻隨機的? 畢竟cluster是依照morton code去切的
	- MaxError是用來幹嘛的?
	- MaxError算是離散程度，就是一個在world space中merge splat跟cluster splat的最大距離 > 其實就是看這段長度在screen space下有多長(是否有在ErrorThreshold的容許範圍)


[Cluster System]
	- Process: Original points > Morton Code > Create Leaf Cluster > Create Parent Cluster > Create LOD splats > save to UGaussianSplatAsset > runtime culling
	- Morton Code: make points adjacent in 3D spce also adjacent in memory(1D Array). Z line adjacent as the final result in 3D space (faster for memory cache). Random order to spatially coherent
	- Create Leaf Cluster: Ater encoded Morton Code and arrange points into 1D Array, for each 128 points sliced into a cluster. Create bounds for frustum culling, screen space error
	- cluster是meta data加在point data上面
	- Create LOD splats: merge 4 points into one points(blending position, rotation and color value)
	- Total Point Count will be larger Than Original PLY. Total Points = Original Splats + LOD Splats
	- splat在decrease的同時，範圍也會變大，照理說不會disappear才對
	- ClusterCulling.usf是主要的LOD選擇算法
	- 紀錄每個cluster最大的離散程度就好 > 這就是MaxError的來由

[LOD splats]
	- 所以LOD splats會包含他是哪一層LOD的資訊?
	- LOD debug mode好像可以看到LOD的資訊?
	- so the final point counts of our uasset will be much larger than the original PLY point count right?
	- Does LOD splats include the LOD data of each cluster? So leaf cluster is LOD 0, the merged higher level cluster will be LOD1, then the merged even higher level cluster will be LOD2, somthing like that?
	- As I remember our debug console command "gs.ShowClusterBounds 2" will show the LOD by color in the level. Is it displaying the color based on the current LOD data stored inside LOD splats?
	

--------------------------------------------------------------------------------------------
[General]
	- this plugin has 2 modules(library/plugin), defined in GaussianSplatting.uplugin file
	- GaussianSplatting is Runtime Type(always), GaussianSplattingEditor is Editor type(editor only, won't ship with game)
	- header files use forward declarations when possible, .cpp files use #include
	- UPrimitiveComponent > can be place in the world and render
	- render workflow: game thread > scene proxy > render thread > shader > GPU
	- BufferRHIRef > GPU side都是這個type
	- GPU的memory跟CPU的是無法共用的
	- shader會用到Buffer裡面的資料比如ViewDataBuffer
	- Any data a shader need will need to create a buffer(in GPU memory)
	- GPU resource is just buffer(static buffer and dynamic buffer)
	- copy the data from RAM into VRAM
	- GPU variable has no type at all, untyped
	- CPU run C++ on RAM, GPU run HLSL on VRAM
	- CPU side UE will use the garbage collector to clean up the memory after actor got deleted
	- GPU side we will need to destroy by ourself
	- 如果沒有destroy會造成memory leak。CPU side是因為有garbage collector
	- UE本身就有一大串cmdlist跟GPU resource task，我只是把它reference過來，加入新funciton，再還回去
	- free VRAM when gaussian splat component got deleted
	- compute shader: not vertex shader, not pixel shader. pure computing, not related to output pixel
	- 用渲染的pixel去決定目前渲染的粗糙程度在畫面中能不能被接受 > 有多少pixel是錯的
	- the file size which has Nanite enabled will be 30% larger

--------------------------------------------------------------------------------------------
[Editor Module]
	- "PLYFileReader": Tools for process PLY binary files. Support reading and parsing data (position, rotation, scale, opacity, color)
	- "GaussianSplatAssetFactory": Import PLY file, build clusters and convert it into custom GaussianSplatAsset .uasset. Support reimport and mark file dirty
	- "GaussianSplatAssetTypeActions": Controls how the imported asset looks and behaves in the content browser editor

--------------------------------------------------------------------------------------------
[Runtime Module]
	[Data]
	- "GaussianDataTypes": Data stuct and enum for GaussianSplatAsset (FGaussianSplatData PLY raw data for each point)
	- "GaussianSplatAsset": .uasset which contains Gaussian Splatting data from PLY files. Fundamental UObject(pure data) + helper functions
	- we use custom .uasset "Gaussian Splat Asset"(serialized, UObject). Won't use original PLY file
	
	[Cluster]
	- "GaussianClusterTypes": define the point number of cluster in each level, helper functions (Encode Morton code...)
	- "GaussianClusterBuilder": define the cluster tree and point reduction ratio. Used in GaussianSplatAssetFactory
	
	[Actor & Component]
	- "GaussianSplatActor": Contain GaussianSplatComponent. Can be placed in the level
	- "GaussianSplatComponent": Game thread. Create component, expose user variables, include GaussianSplatAsset and GaussianSplatSceneProxy
	
	[GPU Data Management]
	- "GaussianSplatSceneProxy": Render thread. Mirror of the GaussianSplatComponent for GPU. Copy the data from bulk data on CPU(RAM), register GPU Resources(buffer), upload buffers to GPU(VRAM), shader use the buffer for calculation, destroy bufer after GaussianSplatComponent got removed
	- A thread safe, render-only copy of the component’s rendering data(UPrimitiveComponent). Render Thread won't touch UComponents
	- pure resource management
	- Buffer(pure GPU memory)
	- ShaderResourceViewRHIRef(SRV)(interpret the memory into type, shader read only)
	- UnorderedAccessViewRHIRef(UAV)(interpret the memory into type, shader read/write)
	- Static buffers: uploaded once from asset data, never change(PositionBuffer, OtherDataBuffer, SHBuffer, ClusterBuffer)
	- Dynamic buffers: allocated once but overwritten every frame by compute shaders(ViewDataBuffer, SortDistanceBuffer, SortKeysBuffer)
	- variable on GPU are untyped

	[Render Pipeline Integration]
	- "GaussianSplatViewExtension": register/unregister GaussianSceneProxy(we don't hook the render into pipeline here)
	- "GaussianSplatting": Module. Incude GaussianSplatViewExtension, build RDG, hook into render pipeline(currently before translucency)
	- each render pipeline stage usually contain lots of passes. RDG will be used for cleaning up the render
	
	[Render]
	- "GaussianSplatShaders": declare shader parameters struct, link with all shader files(.usf)
	- "GaussianSplatRenderer": connect parameters between GaussianSplatSceneProxy and GaussianSplatShaders. Add live view state(camera data) each frame

------------------------------------------------------------------------
[Shader]
	*Shaders will be fire through GaussianSplatRenderer's dispatch functions
	*Buffer were created through GaussianSceneProxy. Shader calculated based on the buffer(reference)
	- "CalcDistances.usf": For each splat, extracts Z coordinate from ViewDataBuffer. Converts depth to sortable uint
	- "RadixSort.usf": GPU gaussian splats sorting
	- "CalcViewData.usf": Transform 3D splat data to 2D screen-space data. Pass calculated result to ViewDataBuffer
	- "GaussianSplatRendering.usf": final render. Rasterrization, vertex shader, pixel shader
	- "GaussianSplatting.ush": some helper functions for loading data from buffer
	- "ClusterCulling.usf": Frustum culling and LOD selection at cluster level for Nanite-style optimization(screen space error)
	- "GaussianDataTypes.ush": some data type
	- "SphericalHarmonics.ush": spherical harmonics calculation

----------------------------------------------------------------------------------------------